File Previewer converts Word, PowerPoint, and Excel files to PDF for preview.

## Features

- Drag-and-drop upload and file picker UI
- Powerbox HTTP API for grain-to-grain conversion
- Server-side conversion using LibreOffice (`unoserver` + `unoconvert`)
- Returns PDF output for previews
